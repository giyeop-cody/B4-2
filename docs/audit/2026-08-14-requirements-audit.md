# 2026-08-14 과제 요구사항 실증 검사

## 1. 검사 목적

이미 만든 결과를 지우지 않고, 과제 원문과 실제 코드·배포 화면을 하나씩 비교했다.
추측으로 통과 표시하지 않고 직접 확인한 것만 기록한다.

## 2. 검사 환경

- 검사일: 2026-08-14 (Asia/Seoul)
- Node.js: 20.20.2
- npm: 10.8.2
- 기준 커밋: `d3482a4`
- 배포 주소: <https://b4-2.vercel.app/>
- 원격 저장소: <https://github.com/giyeop-cody/B4-2>

## 3. 기준선 검사 결과

| 검사 | 방법 | 결과 |
|---|---|---|
| 의존성 설치 | `npm ci` | 통과 |
| 제품 빌드 | `npm run build` | 통과 |
| 보안 검사 | `npm audit --omit=dev` | 실패: 중간 위험도 2건 |
| 자동 테스트 | `npm test` 확인 | 실패: 명령과 테스트가 없음 |
| 배포 첫 화면 | HTTP 요청 | 200 |
| Hash 목록 주소 | `/#/items` 요청 | 200 |
| 일반 목록 주소 | `/items` 요청 | 404. HashRouter 선택에 따른 주소 차이 |
| Supabase CREATE | 검사 전용 행 생성 | 201, 통과 |
| Supabase READ | 생성한 행 ID로 조회 | 200, 통과 |
| Supabase UPDATE | 내용 변경 후 반환값 확인 | 200, 통과 |
| Supabase DELETE | 검사 행 삭제 후 반환값 확인 | 200, 통과 |
| 비밀값 Git 추적 | `git grep` | 저장소 안에서는 발견되지 않음 |
| 정확한 `learning` 브랜치 | 원격 브랜치 목록 | 없음 |
| 정확한 `eval` 브랜치 | 원격 브랜치 목록 | 있음 |

원격 CRUD 검사용 행은 검사 직후 삭제했다. 공개 설정값은 출력하거나 파일에 저장하지 않았다.

## 4. 필수 요구사항 대조

| 요구사항 | 기준선 판단 | 근거 또는 문제 |
|---|---|---|
| React 18 이상 | 통과 | React 18.3.1 |
| 5개 이상 라우트와 404 | 통과 | 6개 라우트와 `NotFoundPage` |
| 목록·상세·등록·수정·삭제 | 부분 통과 | 원격 CRUD는 동작하지만 실패 UI가 일부 페이지에서 보이지 않음 |
| 원격 DB 기준 CRUD | 부분 통과 | 배포 Supabase는 통과. 그러나 설정이 없으면 자동으로 LocalStorage를 써서 원격 실패를 가릴 수 있음 |
| controlled form과 검증 | 통과 | 제목·내용 상태와 필수값 검사 |
| 제출 중 UI | 부분 통과 | 수정 시 훅의 `loading` 때문에 폼 전체가 스피너로 바뀌어 버튼 상태를 보기 어려움 |
| 공통 로딩·에러·빈 상태 | 부분 통과 | `StateView`가 있으나 상세 재시도와 변경 실패 표시가 부족함 |
| pages/components/hooks/lib 분리 | 통과 | 네 폴더가 분리됨 |
| 재사용 컴포넌트 8개 이상 | 통과 | 8개 |
| 커스텀 훅 1개 이상 | 통과 | `useItems`, `useItem` |
| 환경변수 보안 | 부분 통과 | `.env`, `.env.local`은 제외. `.env.production` 패턴은 빠짐 |
| 배포 URL과 README | 통과 | 둘 다 있음 |
| 기능/변경 단위 커밋 | 미흡 | 최초 구현 21개 파일이 한 커밋에 들어감 |
| 이슈 처리 기록 | 미흡 | #1~#5가 구현 후에도 열린 상태이며 새 결함 기록이 없음 |
| `learning`/`eval` 브랜치 | 미흡 | `learning` 이름이 없음 |
| 최종 동료평가 | 미완료 | 준비 문서만 있고 실제 외부 동료 평가 기록은 없음 |

## 5. 보너스 대조

| 보너스 | 기준선 판단 | 할 일 |
|---|---|---|
| 전역 상태 | 누락 | 로그인 사용자를 Context로 관리 |
| 성능 최적화 | 누락 | `React.memo`와 `useMemo` 적용 후 이유 기록 |
| Supabase Auth와 보호 라우트 | 누락 | 로그인/가입, 세션, 프로필 보호 라우트 추가 |

## 6. 문서 오류

- 사용자에게 받은 과제 원문은 학습 시간이 **80시간**인데 `README.md`와 `QUEST.md`에는 **80분**으로 적혀 있다.
- README는 인증을 사용하지 않는다고 적혀 있다. 보너스 구현 뒤 고쳐야 한다.
- LocalStorage 자동 대체를 평가 환경 해결책이라고 설명한다. 필수 조건은 원격 DB이므로 로컬 학습용이라고 범위를 바로잡아야 한다.

## 7. 결론

겉으로 CRUD가 되는 것만으로 끝난 상태는 아니다. 원격 DB 실패를 숨기지 않기, 실패 UI 보이기,
자동 테스트 만들기, 보너스 3개 구현, 브랜치·이슈·평가 기록 보완이 필요하다.

## 8. 2026-08-14 수정 후 재검사

위 3~7장은 최초 기준선의 문제를 보존한 기록이다. 현재 코드는 다음까지 수정했다.

| 항목 | 현재 상태 | 근거 |
|---|---|---|
| 구조·필수 UI | 통과 | 라우트 8개, 페이지 7개, 컴포넌트 11개, 훅 3개 |
| 원격/로컬 모드 구분 | 통과 | LocalStorage는 `VITE_ALLOW_LOCAL_DB=true`인 학습 모드에서만 사용 |
| 변경 실패·제출 상태 | 통과 | 조회와 mutation 오류/진행 상태 분리 |
| 보너스 | 통과 | AuthContext, 보호 라우트, 메모이제이션 구현 |
| 공개 조회·로그인 쓰기 코드 | 통과 | 쓰기 UI, 라우트, API 세션 검사 구현 |
| 단위·컴포넌트 | 통과 | 10개 파일, 28/28 |
| 로컬 브라우저 | 통과 | 2/2 |
| 빌드·보안 | 통과 | Vite build, audit 취약점 0 |
| 데이터베이스 RLS | 통과 | Dashboard 적용 뒤 정책 4개 확인, 익명 SELECT 200·mutation 401 |
| 배포 익명 권한 | 통과 | Production success, Playwright 1/1 |
| 로그인 원격 CRUD | 통과 | 실제 계정으로 등록·수정·삭제·프로필·로그아웃 수동 확인 |
| 실제 외부 동료평가 | **미완료** | 작성 양식만 준비됨 |

최종 권한은 공개 SELECT와 `authenticated` INSERT/UPDATE/DELETE로 적용·검증했다. 작성자별 소유권은 사용자 요청에 따라 범위에서 제외했다. 남은 항목은 실제 외부 동료 3인의 평가와 별도 이슈 #20의 인증 Dashboard URL·새 메일 복귀 링크 확인이다.
